<Window x:Class="MiniEAkte.UI.Views.SignupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MiniEAkte.UI.Views"
        mc:Ignorable="d"
        Title="SignupView" Height="360" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Create an account"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,20">
        </TextBlock>
        <TextBlock Grid.Row="1" 
            Text="Username"/>
        <TextBox Grid.Row="2" Margin="0,0,0,10" Text="{Binding Username}"/>
        <TextBlock Grid.Row="3" 
                   Text="Email"/>
        <TextBox Grid.Row="4" Margin="0,0,0,10" Text="{Binding Email}"/>
        <TextBlock Grid.Row="5" 
                   Text="Password"/>
        <PasswordBox Grid.Row="6" Margin="0,0,0,10" PasswordChanged="PasswordChanged"/>
        <TextBlock Grid.Row="7" 
                   Text="Repeat Password"/>
        <PasswordBox Grid.Row="8" Margin="0,0,0,10" PasswordChanged="RepeatPasswordChanged"/>
        <TextBlock Grid.Row="9"
                   Foreground="Red"
                   Text="{Binding ErrorMessage}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>
        <!-- Login button -->
        <Button Grid.Row="10"
                Content="Sign Up"
                Height="32"
                Command="{Binding SignUpCommand}"/>

    </Grid>
</Window>
