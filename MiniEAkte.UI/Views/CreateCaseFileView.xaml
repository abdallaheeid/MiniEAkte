<Window x:Class="MiniEAkte.UI.Views.CreateCaseFileView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MiniEAkte.UI.Views"
        mc:Ignorable="d"
        Title="CreateCaseFileView" Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="File Number"/>
        <TextBox Grid.Row="1"
                 Text="{Binding FileNumber, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="2" Text="Title" Margin="0,10,0,0"/>
        <TextBox Grid.Row="3"
                 Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="4" Text="Owner" Margin="0,10,0,0"/>
        <TextBox Grid.Row="5"
                 Text="{Binding Owner, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Grid.Row="6"
                   Foreground="Red"
                   Text="{Binding ErrorMessage}" />

        <Button Grid.Row="7" 
                Content="Create"
                HorizontalContentAlignment="Center"
                Margin="0,20,0,0"
                Command="{Binding CreateCommand}" />
    </Grid>
</Window>
