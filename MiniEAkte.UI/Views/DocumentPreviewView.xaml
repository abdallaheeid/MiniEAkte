<Window x:Class="MiniEAkte.UI.Views.DocumentPreviewView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wpf="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MiniEAkte.Application.ViewModels.Documents;assembly=MiniEAkte.Application"
        mc:Ignorable="d"

        Title="Document Preview"
        Height="600"
        Width="900"
        WindowStartupLocation="CenterOwner"

        d:DataContext="{d:DesignInstance vm:DocumentPreviewViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <Button Content="Open Externally"
                    Click="OpenExternally_Click"
                    Margin="0,0,10,0"
                    Padding="5"/>
            <Button Content="Print Document"
                    Margin="0,0,10,0"
                    Padding="5"/>
            <TextBlock Text="{Binding FileName}"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="10" BorderThickness="1" BorderBrush="Gray">
            <wpf:WebView2 x:Name="PdfViewer"/>
        </Border>
    </Grid>
</Window>